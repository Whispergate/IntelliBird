---
phase: 30-notification-channels
plan: 03
type: execute
wave: 3
depends_on: [30-02]
files_modified:
  - backend/app/services/webhook_payloads.py
  - backend/app/services/webhook_dispatcher.py
autonomous: true
requirements: [NOTIF-03, NOTIF-04, NOTIF-05]

must_haves:
  truths:
    - "build_payload_for_type('pagerduty', ...) returns a dict with routing_key placeholder, event_action, dedup_key, and nested payload.severity"
    - "build_payload_for_type('opsgenie', ...) returns a dict with message, alias, priority mapped S→P1"
    - "build_payload_for_type('ntfy', ...) returns a dict with topic extracted from URL path, priority mapped S→5"
    - "_build_auth_headers({'type':'geniekey','api_key':'k'}) returns {'Authorization':'GenieKey k'}"
    - "_build_auth_headers({'type':'bearer','token':'t'}) still returns {'Authorization':'Bearer t'} (unchanged)"
  artifacts:
    - path: "backend/app/services/webhook_payloads.py"
      provides: "3 new payload builders + _BUILDERS dict registration"
      exports: ["build_pagerduty_payload", "build_opsgenie_payload", "build_ntfy_payload"]
    - path: "backend/app/services/webhook_dispatcher.py"
      provides: "GenieKey branch in _build_auth_headers"
  key_links:
    - from: "backend/app/services/webhook_payloads.py"
      to: "backend/app/services/webhook_dispatcher.py"
      via: "build_payload_for_type calls _BUILDERS dict"
      pattern: "_BUILDERS\\[destination_type\\]"
    - from: "backend/app/services/webhook_dispatcher.py"
      to: "Opsgenie API"
      via: "_build_auth_headers geniekey branch"
      pattern: "GenieKey"
---

<objective>
Add three HTTP-based payload builders (PagerDuty, Opsgenie, ntfy) to `webhook_payloads.py` and register them in `_BUILDERS`, plus add the GenieKey auth header branch to `_build_auth_headers` in `webhook_dispatcher.py`.

Purpose: NOTIF-03, NOTIF-04, NOTIF-05 — PagerDuty, Opsgenie, and ntfy all dispatch via the existing `_post_with_retry` path. The builders and auth header extension are the only code changes needed for those three types to flow end-to-end through the existing dispatcher.
Output: 3 new builder functions + _BUILDERS registration + GenieKey auth branch.
</objective>

<execution_context>
@/home/lavender/.claude/get-shit-done/workflows/execute-plan.md
@/home/lavender/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-notification-channels/30-RESEARCH.md
@.planning/phases/30-notification-channels/30-02-SUMMARY.md
</context>

<interfaces>
From backend/app/services/webhook_payloads.py (current _BUILDERS dict, line 212):
```python
_BUILDERS = {
    "slack": build_slack_payload,
    "teams": build_teams_payload,
    "discord": build_discord_payload,
    "generic": build_generic_payload,
}
```

Payload builder signature (match existing builders exactly):
```python
def build_XYZ_payload(
    events: list[dict],
    preset_name: str,
    dashboard_url: str,
    preset_query_params: dict | None = None,
) -> dict[str, Any]:
```

From backend/app/services/webhook_dispatcher.py (current _build_auth_headers, lines 517-536):
```python
def _build_auth_headers(auth_enc: str | None) -> dict[str, str]:
    if not auth_enc: return {}
    creds = decrypt_credentials(settings.SECRET_KEY, auth_enc)
    atype = creds.get("type")
    if atype == "bearer": return {"Authorization": f"Bearer {creds['token']}"}
    if atype == "basic": ...
    if atype == "header": return {creds["name"]: creds["value"]}
    return {}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PagerDuty, Opsgenie, ntfy payload builders in webhook_payloads.py</name>
  <files>backend/app/services/webhook_payloads.py</files>
  <behavior>
    PagerDuty builder:
    - Returns {"routing_key": "_PENDING_INJECTION_", "event_action": "trigger", "dedup_key": str(ev["id"]), "payload": {"summary": ..., "severity": ..., "source": ..., "timestamp": ..., "custom_details": {...}}}
    - routing_key is a placeholder sentinel string "_PENDING_INJECTION_" — Plan 30-05 will inject the real key from auth_enc post-build
    - Severity: S→"critical", A→"error", B→"warning", C/D→"info"
    - Uses events[0] (first event) since PD API accepts one event per POST
    - custom_details includes preset name, stix_type, tlp, tags list, link URL

    Opsgenie builder:
    - Returns {"message": ev.get("title") or "IntelliBird Alert", "alias": str(ev["id"]), "description": ev.get("description") or "", "priority": ..., "source": "IntelliBird", "tags": [preset_name], "details": {"stix_type": ..., "tlp": ..., "link": ...}}
    - Priority: S→"P1", A→"P2", B→"P3", C→"P4", D→"P5"; default "P3"
    - Uses events[0]

    ntfy builder:
    - Returns {"topic": topic, "title": "IntelliBird Alert", "message": ev.get("title") or "IntelliBird Alert", "priority": ..., "tags": ["warning"], "click": link}
    - Topic extracted: `urlparse(dashboard_url).path` is NOT correct — topic must come from the webhook URL, not dashboard_url. Since the builder only receives dashboard_url (not webhook URL), pass the webhook URL via preset_query_params: {"ntfy_url": webhook.url}. Extract: `urlparse(preset_query_params.get("ntfy_url", "")).path.lstrip("/")`; fall back to "intellibird" if empty.
    - Priority: S→5, A→4, B→3, C/D→2; default 3
    - Uses events[0]

    All three registered in _BUILDERS dict:
    - "pagerduty": build_pagerduty_payload
    - "opsgenie": build_opsgenie_payload
    - "ntfy": build_ntfy_payload
    - "email" is NOT registered in _BUILDERS (it bypasses build_payload_for_type entirely)
  </behavior>
  <action>
Read the full `backend/app/services/webhook_payloads.py`. Add the three builder functions after the existing `build_generic_payload` function and before the `_BUILDERS` dict. Extend `_BUILDERS` with the three new keys. Add `from urllib.parse import urlparse` at the top of the file if not already present.

Implement builders exactly as described in the behavior section. Keep them concise (15-25 lines each). Do not add Any imports not already in the file — use existing `from typing import Any` if present.

After _BUILDERS, `build_payload_for_type` function is unchanged.
  </action>
  <verify>
    <automated>cd /home/lavender/Documents/Projects/IntelliBird/backend && uv run pytest tests/unit/test_notif_payloads.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>All test_notif_payloads.py tests pass (not just skipped). The three builder functions are importable and return correctly shaped dicts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GenieKey branch in _build_auth_headers (webhook_dispatcher.py)</name>
  <files>backend/app/services/webhook_dispatcher.py</files>
  <behavior>
    - _build_auth_headers({"type": "geniekey", "api_key": "test_key"}) → {"Authorization": "GenieKey test_key"}
    - _build_auth_headers({"type": "bearer", "token": "t"}) → {"Authorization": "Bearer t"} (unchanged)
    - _build_auth_headers({"type": "basic", "username": "u", "password": "p"}) → Basic auth header (unchanged)
    - _build_auth_headers(None) → {} (unchanged)
    - PagerDuty does NOT use _build_auth_headers — routing_key goes in payload body, not headers (no change needed for PD here)
  </behavior>
  <action>
Read `backend/app/services/webhook_dispatcher.py`. Find the `_build_auth_headers` function (around line 517). After the existing `if atype == "header":` branch, add:

```python
    if atype == "geniekey":
        return {"Authorization": f"GenieKey {creds['api_key']}"}
```

That is the only change to this function. Do NOT change the `bearer` branch — ntfy auth uses `{"type": "bearer", "token": "..."}` which already works via the existing bearer branch.

Do NOT touch `_drain_and_dispatch` in this plan — that comes in Plans 30-04 and 30-05.
  </action>
  <verify>
    <automated>cd /home/lavender/Documents/Projects/IntelliBird/backend && uv run pytest tests/unit/test_notif_dispatch.py::test_build_auth_headers_geniekey tests/unit/test_notif_dispatch.py::test_build_auth_headers_bearer_unchanged -v 2>&1 | tail -15</automated>
  </verify>
  <done>test_build_auth_headers_geniekey passes. test_build_auth_headers_bearer_unchanged passes. No other tests broken.</done>
</task>

</tasks>

<verification>
`cd backend && uv run pytest tests/unit/test_notif_payloads.py tests/unit/test_notif_dispatch.py::test_build_auth_headers_geniekey -v` — all target tests pass.
`uv run python -c "from app.services.webhook_payloads import build_pagerduty_payload, build_opsgenie_payload, build_ntfy_payload; print('ok')"` — exits 0.
</verification>

<success_criteria>
- 3 new payload builder functions in webhook_payloads.py, registered in _BUILDERS
- "email" NOT in _BUILDERS
- _build_auth_headers handles geniekey → GenieKey header
- All test_notif_payloads.py tests pass (unblocked from stub)
- test_build_auth_headers_geniekey passes
</success_criteria>

<output>
After completion, create `.planning/phases/30-notification-channels/30-03-SUMMARY.md`
</output>
