---
phase: 30-notification-channels
plan: 07
type: execute
wave: 6
depends_on: [30-05, 30-06]
files_modified:
  - web/app/webhooks/lib/webhookSchema.ts
  - web/app/webhooks/components/WebhookDialog.tsx
  - web/app/webhooks/components/WebhookTypeBadge.tsx
autonomous: false
requirements: [NOTIF-01, NOTIF-02, NOTIF-03, NOTIF-04, NOTIF-05]

must_haves:
  truths:
    - "Webhook destination type dropdown includes email, pagerduty, opsgenie, ntfy as selectable options"
    - "Selecting email type shows: from_addr, to_addr, SMTP username, password, use_starttls toggle, and smtp:// URL hint"
    - "Selecting pagerduty type shows: routing_key field; URL pre-filled to https://events.pagerduty.com/v2/enqueue"
    - "Selecting opsgenie type shows: api_key field; URL pre-filled to https://api.opsgenie.com/v2/alerts"
    - "Selecting ntfy type shows: token field (optional); URL field labeled as 'topic URL'"
    - "WebhookTypeBadge renders labels and distinct colors for all 4 new types"
    - "tsc --noEmit passes with no new errors beyond pre-existing baseline"
  artifacts:
    - path: "web/app/webhooks/lib/webhookSchema.ts"
      provides: "Widened destination_type zod enum + per-type credential field helpers"
    - path: "web/app/webhooks/components/WebhookDialog.tsx"
      provides: "Conditional credential sections for email/pagerduty/opsgenie/ntfy types"
    - path: "web/app/webhooks/components/WebhookTypeBadge.tsx"
      provides: "Badge labels and colors for 4 new destination types"
  key_links:
    - from: "WebhookDialog.tsx"
      to: "webhookSchema.ts"
      via: "watch('destination_type') conditional rendering"
      pattern: "destination_type.*email|pagerduty|opsgenie|ntfy"
    - from: "webhookSchema.ts buildCreatePayload"
      to: "backend POST /api/admin/webhooks"
      via: "auth_enc JSON shaped per type"
      pattern: "buildCreatePayload"
---

<objective>
Update the frontend webhook admin to support all four new destination types with type-specific credential fields, widened Zod schema validation, and updated type badges.

Purpose: NOTIF-01..05 — The backend now accepts email/pagerduty/opsgenie/ntfy types, but the frontend type dropdown and credential forms still only handle the original 4 types. This plan makes all 4 new types operable through the admin UI.
Output: Updated webhookSchema.ts, WebhookDialog.tsx, WebhookTypeBadge.tsx.
</objective>

<execution_context>
@/home/lavender/.claude/get-shit-done/workflows/execute-plan.md
@/home/lavender/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-notification-channels/30-RESEARCH.md
@.planning/phases/30-notification-channels/30-05-SUMMARY.md
@.planning/phases/30-notification-channels/30-06-SUMMARY.md
</context>

<interfaces>
From web/app/webhooks/lib/webhookSchema.ts (current, line 24):
```typescript
destination_type: z.enum(["slack", "teams", "discord", "generic"] as const),
```
Extend to:
```typescript
destination_type: z.enum(["slack", "teams", "discord", "generic", "email", "pagerduty", "opsgenie", "ntfy"] as const),
```

Current auth_enc shaping in buildCreatePayload/buildUpdatePayload — only handles "generic" type.
New per-type auth_enc shapes:
- email: {"username": ..., "password": ..., "from_addr": ..., "to_addr": ..., "use_starttls": bool}
- pagerduty: {"routing_key": ...}
- opsgenie: {"type": "geniekey", "api_key": ...}
- ntfy: {"type": "bearer", "token": ...} if token provided, else null

Default URL pre-fills:
- pagerduty: "https://events.pagerduty.com/v2/enqueue"
- opsgenie: "https://api.opsgenie.com/v2/alerts"
- ntfy: "" (operator supplies topic URL)
- email: "" (operator supplies smtp://host:port)

WebhookTypeBadge colors:
- email: blue/indigo
- pagerduty: green (PD brand color)
- opsgenie: orange (Opsgenie brand)
- ntfy: purple
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen Zod schema + per-type auth_enc builders in webhookSchema.ts</name>
  <files>web/app/webhooks/lib/webhookSchema.ts</files>
  <behavior>
    - destination_type z.enum accepts all 8 types; validation rejects anything else
    - buildCreatePayload called with destination_type="email" returns auth_enc JSON with {username, password, from_addr, to_addr, use_starttls}
    - buildCreatePayload with destination_type="pagerduty" returns auth_enc JSON with {routing_key}
    - buildCreatePayload with destination_type="opsgenie" returns auth_enc JSON with {type:"geniekey", api_key}
    - buildCreatePayload with destination_type="ntfy" returns auth_enc JSON with {type:"bearer", token} if token provided, else null auth_enc
    - url field for pagerduty defaults to "https://events.pagerduty.com/v2/enqueue" in form defaultValues
    - url field for opsgenie defaults to "https://api.opsgenie.com/v2/alerts"
  </behavior>
  <action>
Read `web/app/webhooks/lib/webhookSchema.ts` in full. Make these changes:

1. Widen the `destination_type` z.enum on the webhook form schema to include all 8 types.

2. Add per-type optional fields to the form schema (use z.string().optional() for new credential fields):
   - `email_from`: string optional
   - `email_to`: string optional
   - `email_username`: string optional
   - `email_password`: string optional
   - `email_use_starttls`: boolean optional (default true)
   - `pd_routing_key`: string optional
   - `opsgenie_api_key`: string optional
   - `ntfy_token`: string optional

3. In `buildCreatePayload` (and `buildUpdatePayload` if it exists), add branches for each new type that produce the correct `auth_enc` JSON shape. Read the existing "generic" branch as a template.

4. In default URL values, add `pagerduty` → `"https://events.pagerduty.com/v2/enqueue"` and `opsgenie` → `"https://api.opsgenie.com/v2/alerts"` in whatever defaultValues or url-preset logic exists.

Match the existing code style exactly (no new utility imports beyond what's already used).
  </action>
  <verify>
    <automated>cd /home/lavender/Documents/Projects/IntelliBird/web && npx tsc --noEmit 2>&1 | grep -v "error TS" | head -5 || npx tsc --noEmit 2>&1 | grep webhookSchema</automated>
  </verify>
  <done>webhookSchema.ts compiles without new TypeScript errors. All 8 destination types in the Zod enum. Per-type auth_enc builders present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Conditional credential sections in WebhookDialog + type badges</name>
  <files>
    web/app/webhooks/components/WebhookDialog.tsx
    web/app/webhooks/components/WebhookTypeBadge.tsx
  </files>
  <behavior>
    WebhookDialog:
    - When destination_type === "email": render form fields for from_addr (required), to_addr (required), username (optional), password (optional), use_starttls toggle (default checked), URL field labeled "SMTP URL (smtp://host:587)"
    - When destination_type === "pagerduty": render routing_key input (required), URL field pre-filled and editable
    - When destination_type === "opsgenie": render api_key input (required), URL field pre-filled and editable, EU endpoint hint text
    - When destination_type === "ntfy": render token input (optional, labeled "Bearer token for private topics"), URL field labeled "Topic URL (e.g. https://ntfy.sh/my-topic)"
    - When destination_type === "slack"|"teams"|"discord": existing URL-only sections unchanged
    - When destination_type === "generic": existing auth sections unchanged
    - Use watch("destination_type") from react-hook-form to control conditional rendering

    WebhookTypeBadge:
    - email → indigo badge, label "Email"
    - pagerduty → green badge, label "PagerDuty"
    - opsgenie → orange badge, label "Opsgenie"
    - ntfy → purple badge, label "ntfy"
    - existing types unchanged
  </behavior>
  <action>
Read `web/app/webhooks/components/WebhookDialog.tsx` in full to understand the form structure, existing conditional rendering for "generic" type auth, and shadcn/ui component patterns used (FormField, FormItem, FormLabel, FormControl, Input, Switch, etc.).

Read `web/app/webhooks/components/WebhookTypeBadge.tsx` (or equivalent badge component) to understand the existing badge color map pattern.

WebhookDialog changes:
1. Use `const destType = watch("destination_type")` to get live type value.
2. Add a `{destType === "email" && (...)}` conditional JSX block in the form for the 5 email fields. Mirror the style of the existing generic auth section.
3. Add similar blocks for pagerduty (routing_key), opsgenie (api_key + EU hint), ntfy (optional token).
4. For pagerduty and opsgenie, set the URL field's defaultValue in the useEffect or wherever defaultValues are applied.

WebhookTypeBadge changes:
1. Find the existing switch/object that maps destination_type → label and variant/color.
2. Add the 4 new type entries.

Do NOT redesign the dialog — extend existing patterns only. Keep each new section minimal (2-4 form fields per type).
  </action>
  <verify>
    <automated>cd /home/lavender/Documents/Projects/IntelliBird/web && npx tsc --noEmit 2>&1 | grep -c "error TS" || true</automated>
  </verify>
  <done>TypeScript error count does not increase beyond pre-existing baseline (2 known errors in webhook tests). WebhookDialog and WebhookTypeBadge compile cleanly. All 4 new type sections present in WebhookDialog.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — new destination types in webhook admin UI</name>
  <what-built>
    Frontend webhook admin updated with 4 new destination type credential sections (email, pagerduty, opsgenie, ntfy) and type badges.
  </what-built>
  <how-to-verify>
    1. Open http://localhost:3000/webhooks (or your IntelliBird instance)
    2. Click "Add Webhook" — verify type dropdown lists all 8 types including email, pagerduty, opsgenie, ntfy
    3. Select "email" — verify SMTP credential fields appear (from, to, username, password, STARTTLS toggle)
    4. Select "pagerduty" — verify routing_key field + URL pre-filled to PD endpoint
    5. Select "opsgenie" — verify api_key field + URL pre-filled + EU hint
    6. Select "ntfy" — verify optional token field + topic URL label
    7. Verify existing "slack" type still shows URL-only form (no regression)
    8. In webhook list, verify type badges show correct labels and colors for new types
  </how-to-verify>
  <resume-signal>Type "approved" if the UI matches the above, or describe any issues</resume-signal>
  <action>Human verification step — no automated action. Executor pauses here for user to test the webhook admin UI in the browser.</action>
</task>

</tasks>

<verification>
`cd web && npx tsc --noEmit 2>&1 | grep webhookSchema` — no new errors.
`grep -c "email\|pagerduty\|opsgenie\|ntfy" web/app/webhooks/lib/webhookSchema.ts` — multiple matches.
Manual UI verify via checkpoint.
</verification>

<success_criteria>
- webhookSchema.ts Zod enum includes all 8 types
- Per-type auth_enc builders produce correct JSON shapes
- WebhookDialog shows conditional credential sections for each new type
- WebhookTypeBadge shows correct labels/colors for all 4 new types
- TypeScript compiles with no new errors
- Human verifies UI in browser (checkpoint approved)
</success_criteria>

<output>
After completion, create `.planning/phases/30-notification-channels/30-07-SUMMARY.md`
</output>
