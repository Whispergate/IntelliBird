---
phase: 30-notification-channels
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/tests/unit/test_notif_payloads.py
  - backend/tests/unit/test_notif_email.py
  - backend/tests/unit/test_notif_dispatch.py
autonomous: true
requirements: [NOTIF-01, NOTIF-02, NOTIF-03, NOTIF-04, NOTIF-05]

must_haves:
  truths:
    - "pytest discovers all three test stubs and collects them (0 failures, all skipped/xfail)"
    - "`aiosmtplib>=3.0,<6` resolves and imports inside the backend container"
  artifacts:
    - path: "backend/tests/unit/test_notif_payloads.py"
      provides: "Stub tests for PD, Opsgenie, ntfy payload builders"
    - path: "backend/tests/unit/test_notif_email.py"
      provides: "Stub tests for email dispatch via aiosmtplib"
    - path: "backend/tests/unit/test_notif_dispatch.py"
      provides: "Stub tests for dispatcher routing + GenieKey headers + PD routing_key body injection"
    - path: "backend/pyproject.toml"
      provides: "aiosmtplib>=3.0,<6 dependency"
  key_links:
    - from: "backend/pyproject.toml"
      to: "backend/tests/unit/test_notif_email.py"
      via: "import aiosmtplib"
      pattern: "aiosmtplib"
---

<objective>
Wave 0 foundation: add `aiosmtplib` to pyproject.toml and create three test stub files that establish the Nyquist-compliant test targets for all NOTIF requirements. Tests are written as `pytest.mark.skip` stubs so the suite stays green while implementations land in waves 2-6.

Purpose: Every subsequent plan has an `<automated>` verify command pointing at these files. Without this wave, later plans cannot satisfy the Nyquist rule.
Output: Three test stub files + updated pyproject.toml dependency list.
</objective>

<execution_context>
@/home/lavender/.claude/get-shit-done/workflows/execute-plan.md
@/home/lavender/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-notification-channels/30-CONTEXT.md
@.planning/phases/30-notification-channels/30-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add aiosmtplib to pyproject.toml</name>
  <files>backend/pyproject.toml</files>
  <behavior>
    - `import aiosmtplib` succeeds after `uv sync`
    - Version range `>=3.0,<6` allows aiosmtplib 3.x, 4.x, 5.x
  </behavior>
  <action>
Read backend/pyproject.toml. In the `[project] dependencies` array, add `"aiosmtplib>=3.0,<6"` after the existing aiohttp/httpx entries (alphabetical or after http-related deps). Do NOT add it to dev/test extras. Run `uv lock --no-upgrade` inside `backend/` to regenerate uv.lock without bumping other packages.

Do NOT run `uv sync` — the lock file regeneration is sufficient; the container image rebuild is the operator's responsibility.
  </action>
  <verify>
    <automated>cd /home/lavender/Documents/Projects/IntelliBird/backend && grep 'aiosmtplib' pyproject.toml</automated>
  </verify>
  <done>pyproject.toml contains `aiosmtplib>=3.0,<6` and uv.lock is regenerated with no other version bumps</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create unit test stub files for all NOTIF requirements</name>
  <files>
    backend/tests/unit/test_notif_payloads.py
    backend/tests/unit/test_notif_email.py
    backend/tests/unit/test_notif_dispatch.py
  </files>
  <behavior>
    Payload stubs (test_notif_payloads.py):
    - test_build_pagerduty_payload — NOTIF-03: builder returns dict with "event_action", "dedup_key", nested "payload.severity"
    - test_pagerduty_severity_mapping — S→critical, A→error, B→warning, C/D→info
    - test_pagerduty_routing_key_in_body — routing_key present in returned dict (not header)
    - test_build_opsgenie_payload — NOTIF-05: returns dict with "message", "alias", "priority"
    - test_opsgenie_priority_mapping — S→P1, A→P2, B→P3, C→P4, D→P5
    - test_build_ntfy_payload — NOTIF-04: returns dict with "topic", "title", "message", "priority"
    - test_ntfy_topic_extracted_from_url — URL "https://ntfy.sh/my-topic" → topic="my-topic"
    - test_ntfy_priority_mapping — S→5, A→4, B→3, C/D→2

    Email stubs (test_notif_email.py):
    - test_dispatch_email_success — NOTIF-02: mocked aiosmtplib.send returns success → (True, None)
    - test_dispatch_email_starttls — use_starttls=true → start_tls=True, use_tls=False
    - test_dispatch_email_implicit_tls — use_starttls=false → start_tls=False, use_tls=True
    - test_dispatch_email_failure — aiosmtplib raises SMTPException → (False, str(error))
    - test_email_subject_format — subject = "[IntelliBird] N alert(s) — preset_name"

    Dispatch stubs (test_notif_dispatch.py):
    - test_build_auth_headers_geniekey — NOTIF-05: {"type":"geniekey","api_key":"x"} → {"Authorization":"GenieKey x"}
    - test_build_auth_headers_bearer_unchanged — existing bearer branch still works
    - test_drain_dispatch_email_branch — destination_type="email" calls _dispatch_email, NOT _post_with_retry
    - test_pd_auto_resolve — archiver hook fires HTTP POST with event_action="resolve" on archived events
  </behavior>
  <action>
Create all three test stub files under `backend/tests/unit/`. Mark every test function with `@pytest.mark.skip(reason="stub — implementation pending")`. Import the target functions at the top of each file using their final module paths (even though the functions don't exist yet — this will cause ImportError until the implementations land, which is acceptable for a stub). Use `try/except ImportError: pytest.skip(...)` guards at module level so the collection pass does not abort the entire suite.

Structure:
```
test_notif_payloads.py:
  try: from app.services.webhook_payloads import build_pagerduty_payload, build_opsgenie_payload, build_ntfy_payload
  except ImportError: pytestmark = pytest.mark.skip("impl pending")

test_notif_email.py:
  try: from app.services.webhook_dispatcher import _dispatch_email
  except ImportError: pytestmark = pytest.mark.skip("impl pending")

test_notif_dispatch.py:
  try: from app.services.webhook_dispatcher import _build_auth_headers, _drain_and_dispatch
  except ImportError: pytestmark = pytest.mark.skip("impl pending")
```

Write each test as a stub function body: `pytest.skip("stub")`.
  </action>
  <verify>
    <automated>cd /home/lavender/Documents/Projects/IntelliBird/backend && uv run pytest tests/unit/test_notif_payloads.py tests/unit/test_notif_email.py tests/unit/test_notif_dispatch.py -v --no-header 2>&1 | tail -20</automated>
  </verify>
  <done>All test stubs are collected by pytest with 0 errors (all either skipped or marked skip). No ImportError crashes the collection run.</done>
</task>

</tasks>

<verification>
`cd backend && uv run pytest tests/unit/test_notif_payloads.py tests/unit/test_notif_email.py tests/unit/test_notif_dispatch.py -v` — all collected, none ERROR.
`grep 'aiosmtplib' backend/pyproject.toml` — line present.
</verification>

<success_criteria>
- pyproject.toml contains `aiosmtplib>=3.0,<6`
- uv.lock regenerated without other version bumps
- 3 test stub files exist under backend/tests/unit/
- `pytest ... -v` collects 17 test items, all skipped or skip-marked, 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/30-notification-channels/30-01-SUMMARY.md`
</output>
