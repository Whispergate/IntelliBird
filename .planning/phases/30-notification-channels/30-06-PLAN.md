---
phase: 30-notification-channels
plan: 06
type: execute
wave: 5
depends_on: [30-02]
files_modified:
  - ops/docker-compose.yml
autonomous: true
requirements: [NOTIF-04]

must_haves:
  truths:
    - "`docker compose --profile notify config` validates without errors"
    - "ntfy service defined under profiles: [notify] with port 2586:80 and persistent volumes"
    - "ntfy-cache and ntfy-etc volumes declared in top-level volumes block"
  artifacts:
    - path: "ops/docker-compose.yml"
      provides: "ntfy compose service under profiles: [notify]"
      contains: "binwiederhier/ntfy"
  key_links:
    - from: "ops/docker-compose.yml ntfy service"
      to: "ntfy HTTP API"
      via: "port 2586:80 mapping, volumes ntfy-cache + ntfy-etc"
      pattern: "ntfy.*notify"
---

<objective>
Add the ntfy self-hosted service to `ops/docker-compose.yml` under `profiles: ["notify"]`, consistent with the existing `darkweb` and `sso` profile patterns.

Purpose: NOTIF-04 — Admin must be able to run `docker compose --profile notify up` to get a local ntfy instance. The ntfy service uses port 2586 (avoids clash with other common ports) and persists data via named volumes.
Output: Updated docker-compose.yml with ntfy service + 2 new volumes.
</objective>

<execution_context>
@/home/lavender/.claude/get-shit-done/workflows/execute-plan.md
@/home/lavender/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-notification-channels/30-RESEARCH.md
@.planning/phases/30-notification-channels/30-02-SUMMARY.md
</context>

<interfaces>
From ops/docker-compose.yml — existing darkweb profile pattern (to clone):
```yaml
tor:
  image: ${TOR_IMAGE:-osminogin/tor-simple:latest}
  profiles: ["darkweb"]
  networks:
    - darkweb_net
  restart: unless-stopped
```

ntfy service to add (from RESEARCH.md Pattern 6):
```yaml
ntfy:
  image: ${NTFY_IMAGE:-binwiederhier/ntfy:latest}
  profiles: ["notify"]
  command: serve
  volumes:
    - ntfy-cache:/var/cache/ntfy
    - ntfy-etc:/etc/ntfy
  ports:
    - "${NTFY_PORT:-2586}:80"
  restart: unless-stopped
```
Add ntfy-cache and ntfy-etc to the top-level volumes block.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add ntfy service and volumes to docker-compose.yml</name>
  <files>ops/docker-compose.yml</files>
  <action>
Read `ops/docker-compose.yml` in full to understand the existing structure and placement.

Add the ntfy service in the `services:` block. Place it near the end of the services section, after other profile-gated services (tor, authentik, etc.) but before the final closing of the services block. Add a comment block before the service:

```yaml
  # ---------------------------------------------------------------------------
  # ntfy notification service (optional — profile: notify)
  # Run: docker compose --profile notify up
  # Self-hosted push notification broker for NOTIF-04.
  # Default port 2586; override with NTFY_PORT env var.
  # ---------------------------------------------------------------------------
  ntfy:
    image: ${NTFY_IMAGE:-binwiederhier/ntfy:latest}
    profiles: ["notify"]
    command: serve
    volumes:
      - ntfy-cache:/var/cache/ntfy
      - ntfy-etc:/etc/ntfy
    ports:
      - "${NTFY_PORT:-2586}:80"
    restart: unless-stopped
```

In the top-level `volumes:` block (at the bottom of the file), add:
```yaml
  ntfy-cache:
  ntfy-etc:
```

Do NOT add ntfy to any network (it is clearnet push, no isolation needed — unlike tor which has its own network).

Do NOT change any existing services, volumes, or networks.
  </action>
  <verify>
    <automated>cd /home/lavender/Documents/Projects/IntelliBird && docker compose -f ops/docker-compose.yml --profile notify config --quiet 2>&1 | head -5 || echo "docker compose validate exit $?"</automated>
  </verify>
  <done>docker-compose.yml contains ntfy service under profiles: [notify] with correct volumes and port. ntfy-cache and ntfy-etc volumes declared. docker compose config validates without errors.</done>
</task>

</tasks>

<verification>
`grep -A 12 'ntfy:' ops/docker-compose.yml` — service block present with profiles, command, volumes, ports.
`grep 'ntfy-cache\|ntfy-etc' ops/docker-compose.yml | wc -l` — at least 4 (2 in service volumes, 2 in top-level volumes).
</verification>

<success_criteria>
- ntfy service in docker-compose.yml under profiles: [notify]
- Port mapping ${NTFY_PORT:-2586}:80
- Volumes ntfy-cache + ntfy-etc declared in top-level volumes block
- docker compose config validates without errors
</success_criteria>

<output>
After completion, create `.planning/phases/30-notification-channels/30-06-SUMMARY.md`
</output>
