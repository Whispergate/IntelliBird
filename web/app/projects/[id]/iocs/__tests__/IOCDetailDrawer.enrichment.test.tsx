/**
 * IOCDetailDrawer enrichment section stubs — Wave 0
 * Enrichment section does not exist yet; tests are todos until plan 23-06.
 */
import { describe, test } from "vitest";

describe("IOCDetailDrawer — Enrichment section", () => {
  test.todo("renders per-provider rows with verdict pill, score, fetched_at relative time");
  test.todo("renders 'no provider configured' when enrichments array is empty");
  test.todo("Refresh button visible only for Lead+ role");
  test.todo("Refresh button calls triggerIOCEnrichment with ?refresh=true");
  test.todo("verdict pill colours: malicious=red, suspicious=amber, clean=green, unknown=muted");
});
